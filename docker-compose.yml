services:
  metriplate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metriplate_container
    ports:
      - '8080:8080'
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    env_file:
      - .env
